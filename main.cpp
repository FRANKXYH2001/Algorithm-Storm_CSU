#include <iostream>
#include <bits/stdc++.h>

using namespace std;

int main() {
    /*-------------------------------------------------------------------------------------*/
    /*  STL容器常用方法(method)
     *  .empty()   // 判断当前容器是否为空
     *  .size()    // 返回当前容器实际元素个数
     *  .begin()   // 返回iterator(迭代器),引用容器的第一个元素
     *  .end()     // 返回iterator(迭代器),引用容器的最后一个元素的后一个位置(边界)*/
    /*-------------------------------------------------------------------------------------*/



    return 0;
}
